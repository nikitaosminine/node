# CLAUDE.md

The project instructions for this repository live in [AGENTS.md](./AGENTS.md).

Read `AGENTS.md` before working in this repo — it is the single source of truth for architecture,
commands, deployment rules and coding standards. This file is a pointer only; do not add guidance
here, and update `AGENTS.md` instead.

(It is a plain file rather than a symlink on purpose: a symlink degrades to a 9-byte text stub on
Windows checkouts without symlink support, which would leave agents with no instructions at all.)
